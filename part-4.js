var rs = require('readline-sync');
//duplicate creating ships but assign to different variable for the computer. 
//create another version of checkIfHit that doesn't ask for input, but just runs right after I guess.
//additional checkIfHit fn checks against the computer's ships instead of mine but everything else should be the same.
//place both functions together in the game loop to alternate and end both once one team wins.
//udpdate the wonGame method to display different message if computer won